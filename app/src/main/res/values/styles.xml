<resources>

    <style name="BaseAppStyle" parent="Theme.AppCompat.Light.NoActionBar">

    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="BaseAppStyle">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorActionBarBgColor</item>
        <item name="colorPrimaryDark">@color/colorActionBarBgColor</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorActBg</item>
        <item name="android:actionMenuTextColor">@android:color/white</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!-- ToolBar菜单样式 -->
    <style name="actionBarTheme" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="actionMenuTextColor">@android:color/white</item> <!--  敲定颜色-->
        <item name="android:textSize">18sp</item> <!--  搞掂字体大小-->
    </style>

    <style name="LaunchAppTheme" parent="BaseAppStyle">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@mipmap/launch_img</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>

    </style>
    <style name="login_edit">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/login_margin</item>
        <item name="android:layout_marginRight">@dimen/login_margin</item>
        <item name="android:maxLines">1</item>
    </style>

    <style name="login_btn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginLeft">@dimen/login_margin</item>
        <item name="android:layout_marginRight">@dimen/login_margin</item>
        <item name="android:maxLines">1</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!--这里设置 tablayout textview的一些属性-->
    <style name="styleMainTabLayoutTextView" parent="TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse">
        <item name="android:textSize">18sp</item>
    </style>


    <style name="styleBtnSub">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/btnSubMinHeight</item>
        <item name="android:layout_marginLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginRight">@dimen/activity_horizontal_margin</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

</resources>
